# Twin FastAPI base URL (REST + WebSocket). Local default when developing:
VITE_TWIN_API_URL=http://127.0.0.1:8000

# Optional override for remote twin list (/api/twins). Falls back to VITE_TWIN_API_URL
# or the hardcoded production host when unset.
# VITE_TWIN_DATA_API_URL=http://127.0.0.1:8000
